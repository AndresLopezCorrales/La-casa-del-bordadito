<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/Label_contacto_redes"
        android:textColor="@color/terracotta"
        android:textSize="11sp"
        android:textStyle="bold"
        android:letterSpacing="0.12"
        android:layout_marginBottom="10dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_card_bone"
        android:clipToOutline="true"
        android:padding="8dp">

        <!-- Instagram -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnInstagram"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/Info_instagram"
            android:textColor="@color/dark_coffee"
            android:gravity="start|center_vertical"
            android:textAlignment="viewStart"
            app:icon="@drawable/ic_inicio"
            app:iconTint="@color/terracotta" />

        <View android:layout_width="match_parent" android:layout_height="1dp" android:background="@color/silver" android:layout_marginHorizontal="12dp" />

        <!-- WhatsApp / Celular -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnWhatsApp"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/Info_whatsapp"
            android:textColor="@color/dark_coffee"
            android:gravity="start|center_vertical"
            android:textAlignment="viewStart"
            app:icon="@drawable/ic_inicio"
            app:iconTint="@color/sage_green" />

        <View android:layout_width="match_parent" android:layout_height="1dp" android:background="@color/silver" android:layout_marginHorizontal="12dp" />

        <!-- Email -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnEmailContacto"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/Info_email_contacto"
            android:textColor="@color/dark_coffee"
            android:gravity="start|center_vertical"
            android:textAlignment="viewStart"
            app:icon="@drawable/ic_email"
            app:iconTint="@color/brown" />

    </LinearLayout>

</LinearLayout>