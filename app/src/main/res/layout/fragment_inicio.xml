<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Fragmentos.FragmentInicio">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="100dp">

            <!-- Slider de Imágenes -->
            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/viewPagerSlider"
                android:layout_width="match_parent"
                android:layout_height="220dp"
                android:layout_marginTop="16dp"
                android:layout_marginHorizontal="16dp" />

            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tabIndicator"
                android:layout_width="wrap_content"
                android:layout_height="12dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="8dp"
                android:background="@android:color/transparent"
                app:tabBackground="@drawable/tab_selector"
                app:tabGravity="center"
                app:tabIndicatorHeight="0dp"
                app:tabMaxWidth="12dp" />

            <!-- Selector de Contenido (Toggle) -->
            <com.google.android.material.button.MaterialButtonToggleGroup
                android:id="@+id/toggleGroup"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="24dp"
                app:checkedButton="@id/btnToggleCafe"
                app:selectionRequired="true"
                app:singleSelection="true">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnToggleCafe"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Cafés"
                    app:icon="@drawable/ic_cafe"
                    app:backgroundTint="@color/toggle_bg_selector"
                    android:textColor="@color/toggle_text_icon_selector"
                    app:iconTint="@color/toggle_text_icon_selector"
                    app:strokeColor="@color/silver" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnToggleBordado"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Bordados"
                    app:icon="@drawable/ic_bordado"
                    app:backgroundTint="@color/toggle_bg_selector"
                    android:textColor="@color/toggle_text_icon_selector"
                    app:iconTint="@color/toggle_text_icon_selector"
                    app:strokeColor="@color/silver" />

            </com.google.android.material.button.MaterialButtonToggleGroup>

            <!-- Poster de Bordado (Visible solo cuando se selecciona Bordados) -->
            <include
                android:id="@+id/layoutPosterBordado"
                layout="@layout/item_bordado_inicio"
                android:visibility="gone" />

            <!-- Header Cafés con Selector de Vista -->
            <LinearLayout
                android:id="@+id/layoutHeaderCafe"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingHorizontal="16dp"
                android:layout_marginTop="24dp"
                android:visibility="visible">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Nuestros Cafés"
                    android:textAppearance="?attr/textAppearanceTitleMedium"
                    android:textColor="@color/dark_coffee"
                    android:textStyle="bold" />

                <com.google.android.material.button.MaterialButtonToggleGroup
                    android:id="@+id/toggleViewType"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:checkedButton="@id/btnGrid"
                    app:selectionRequired="true"
                    app:singleSelection="true">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnGrid"
                        style="@style/Widget.Material3.Button.OutlinedButton"
                        android:layout_width="48dp"
                        android:layout_height="40dp"
                        android:padding="0dp"
                        app:icon="@drawable/ic_grid_view"
                        app:iconGravity="textStart"
                        app:iconPadding="0dp"
                        app:backgroundTint="@color/toggle_bg_selector"
                        app:iconTint="@color/toggle_text_icon_selector"
                        app:strokeColor="@color/silver" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnList"
                        style="@style/Widget.Material3.Button.OutlinedButton"
                        android:layout_width="48dp"
                        android:layout_height="40dp"
                        android:padding="0dp"
                        app:icon="@drawable/ic_list_view"
                        app:iconGravity="textStart"
                        app:iconPadding="0dp"
                        app:backgroundTint="@color/toggle_bg_selector"
                        app:iconTint="@color/toggle_text_icon_selector"
                        app:strokeColor="@color/silver" />

                </com.google.android.material.button.MaterialButtonToggleGroup>

            </LinearLayout>

            <!-- Lista de Contenido -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerInicio"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:clipToPadding="false"
                android:nestedScrollingEnabled="false"
                android:paddingHorizontal="8dp"
                app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
                app:spanCount="2"
                tools:listitem="@layout/item_cafe_no_desc" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>